# AI AGENT Rules
---

General Rules:

- never run npm build or dev commands. During the task, the human will build and test the implementations
- Always use types, custom if needed. Avoid using any. If you don't know the type, use unknown.